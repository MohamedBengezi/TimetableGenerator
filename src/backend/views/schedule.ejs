<!DOCTYPE html>
<html>

<head>
    <title>Schedule</title>
    <link rel="icon" href="https://www.iconsfind.com/wp-content/uploads/2015/11/20151104_563963802fd08-210x210.png">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="/javascripts/jquery.js"></script>
    <script src="/javascripts/schedule.js"></script>
</head>
<body>

    <% var counter1 = 1; schedule.forEach(function (version) { %>
<ul>
    <div>
        <h1>Semester <%= counter1%> <% var idName ='schedule' + counter1++ %></h1>
        <button type="button" class="btn btn-primary" id="generate<%= counter1 %>">Generate Again </button>
    </div>
    <br>

    <table id=<%= idName%> style="width:100%" border="1">
        <tr>
            <th>Time</th>
            <th>Monday</th>
            <th>Tuesday</th>
            <th>Wednesday</th>
            <th>Thursday</th>
            <th>Friday</th>
            <th>Saturday</th>
        </tr>

        <% for(var i =8; i < 22.5; i += 0.5){var id = 'time' +i+'';id= id.replace('.','')%>
            <tr class=<%= id%>>
                <td style="border: 1px solid black; border-collapse: collapse; text-align: center">
                    <%
                    var time = i + '';
                    if(time.includes('.5')){
                        var s = i - 0.5 + ':30'; %>
                        <%= s%>
                    <%}
                    else{
                        var s = i  + ':00'%>
                    <%= s%>
                    <% }%>
                </td>
                <% for (var j =0; j < 6; j++){ var id="day"+j;%>
                <td class= <%= id%> style="text-align:center"></td>
                <%}%>
            </tr>

        <% } %>
    </table>

</ul>
    <%})%>

</body>
</html>